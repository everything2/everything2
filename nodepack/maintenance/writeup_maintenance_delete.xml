<node>
  <type_nodetype>150</type_nodetype>
  <maintain_nodetype>117</maintain_nodetype>
  <node_id>167264</node_id>
  <title>writeup maintenance delete</title>
  <maintaintype>delete</maintaintype>
  <code># although writeups should not be nuked, the opcode is still there, and gods may
# use it. So to avoid writeups getting nuked, we turn them into drafts first.
# (Then the draft gets nuked. )

htmlcode('unpublishwriteup', $_[0], '(nuked)');
</code>
</node>
