<node>
  <type_nodetype>150</type_nodetype>
  <delegate_to></delegate_to>
  <maintain_nodetype>1151141</maintain_nodetype>
  <node_id>1415704</node_id>
  <title>patch update maint</title>
  <maintaintype>update</maintaintype>
  <code>my ($PATCH) = @_;
getRef($PATCH);
my $pf = getVars(getNode(&quot;patchable fields&quot;, &quot;setting&quot;));

# I'm not sure this check is necessary here, since it's already done in maint create.
# Commenting it out --[Swap]

#unless($pf and $$pf{$$PATCH{field}})
#{ 
#  nukeNode($PATCH, -1, 1);
#  return;
#}</code>
</node>
