<node>
  <type_nodetype>2</type_nodetype>
  <parent_container>0</parent_container>
  <node_id>1225502</node_id>
  <context>[%
#THIS IS A WORK IN PROGRESS
#this was created so the refreshing chatterbox nodelet doesn't reload the entire page, but it should be usable with anything
#TODO: this keeps the background color of each theme, so the iframe-d nodelet doesn't look out of place
return '';
#TODO: would base href work with frame? have to do this, else links won't work properly
%]
&lt;html&gt;
&lt;head&gt;
&lt;title&gt;[&quot;$$NODE{title}&quot;]&lt;/title&gt;&lt;!-- TODO theme-specific --&gt;
&lt;/head&gt;
[{ekw userbody}]&lt;!-- FIXME although this works OK with default theme --&gt;
CONTAINED_STUFF
&lt;/body&gt;
&lt;/html&gt;</context>
  <title>iframe container</title>
</node>
