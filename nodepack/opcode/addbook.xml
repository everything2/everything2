<node>
  <type_nodetype>415056</type_nodetype>
  <node_id>1547879</node_id>
  <title>addBook</title>
  <code>foreach my $bnode ($query-&gt;param())
{
  next unless($bnode =~ /^bnode_([0-9]*)$/);

  $$VARS{bookbucket} .= &quot;,&quot; if($$VARS{bookbucket} &amp;&amp; $$VARS{bookbucket} ne &quot;&quot;);
  $$VARS{bookbucket} .= $1;
}</code>
</node>
