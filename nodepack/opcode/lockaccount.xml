<node>
  <type_nodetype>415056</type_nodetype>
  <delegate_to></delegate_to>
  <node_id>1203049</node_id>
  <title>lockaccount</title>
  <code>return unless(isGod($USER));

my $uid = $query-&gt;param('lock_id');
return unless $uid;
htmlcode('lock user account', $uid);

</code>
</node>
